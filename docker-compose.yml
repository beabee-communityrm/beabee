version: '3.2'

services:
  db:
    image: mongo:3.6.18
    volumes:
      - db_data:/data/db
  app:
    build: .
    volumes:
      - ./.:/membership-system
    ports:
      - 3001:3001

volumes:
  db_data:
