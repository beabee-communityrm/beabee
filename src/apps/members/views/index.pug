extends /views/notrack.pug

block prepend title
	- title = app.title

block append css
	link( rel='stylesheet', href='/static/css/query-builder.default.css' )

block append js
	script( src='/static/js/lib/query-builder.standalone.js' )
	script
		if search.query
			| var searchQuery = !{search.query}
	script( src='/static/js/search.js?v=' + git)

block contents
	.row
		.col-md-12
			+page_header( 'Search' )
			include partials/search.pug
	.row
		.col-md-12
			h3
				strong= title
				|
				|
				small= pagination.start + ' to ' + pagination.end  + ' of ' + total + ' displayed'
			if hasFilter
				form(method='POST')
					+csrf
					p.text-right
						button(name='action' value='save-segment').btn.btn-primary Save segment
			if members.length > 0
				include partials/table.pug
				include partials/pagination.pug
			else
				+messageBlock( 'info', 'There are no members to display.' )
